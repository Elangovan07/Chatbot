To make it work connect it with the SQL database (phpMyadmin);

with,

database name: bot;
user : root;
password: "";
host : localhost;


table name: chatbot;



You need to insert the predefined queries and replies for the chatbot to respond.